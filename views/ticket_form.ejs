<!DOCTYPE html>
<html lang="hr">
<head>
  <meta charset="UTF-8">
  <title>Uplata listića</title>

  <link href="https://fonts.googleapis.com/css2?family=Bebas+Neue&display=swap" rel="stylesheet">

  <style>
    body {
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial, sans-serif;
      margin: 0;
      background: #f2f4f8;
      color: #222;
    }

    .main-wrapper {
      max-width: 600px;
      margin: 60px auto 0 auto;
      padding: 0 20px;
    }

    .box {
      background: #fff;
      border: 1px solid #ddd;
      padding: 24px;
      border-radius: 10px;
      box-shadow: 0 3px 10px rgba(0,0,0,0.05);
    }

    h1 {
      font-family: 'Bebas Neue', sans-serif;
      text-align: center;
      font-size: 44px;
      letter-spacing: 1px;
      margin-top: 0;
      margin-bottom: 20px;
      color: #1a73e8;
    }

    label {
      font-weight: 500;
      display: block;
      margin-bottom: 6px;
      color: #333;
    }

    input[type="text"] {
      width: 100%;
      padding: 10px;
      border: 1px solid #bbb;
      border-radius: 6px;
      font-size: 15px;
      margin-bottom: 16px;
    }

    button {
      background: #1a73e8;
      color: white;
      padding: 10px 16px;
      border: 1px solid #0f5fd1;
      border-radius: 8px;
      cursor: pointer;
      font-size: 15px;
      font-weight: 500;
      transition: 0.18s;
      width: 100%;
    }

    button:hover {
      background: #0f5fd1;
    }

    .back-link {
      display: inline-block;
      margin-top: 20px;
      text-decoration: none;
      color: #1a73e8;
      font-weight: 500;
      text-align: center;
      width: 100%;
    }

    .back-link:hover {
      text-decoration: underline;
    }
  </style>
</head>

<body>
  <div class="main-wrapper">
    <div class="box">
      <h1>Uplata Loto 6/45</h1>

      <form action="/tickets" method="POST">
        <label for="personal_id">Osobna iskaznica / putovnica:</label>
        <input type="text" name="personal_id" id="personal_id" maxlength="20" required>

        <label for="numbers">Unesite 6-10 brojeva (1-45), odvojene zarezom:</label>
        <input type="text" name="numbers" id="numbers" placeholder="npr. 5,12,23,34,40,45" required>

        <input type="hidden" name="roundId" value="<%= roundId %>">

        <button type="submit">Pošalji listić</button>
      </form>

      <a class="back-link" href="/">⟵ Povratak na početnu</a>
    </div>
  </div>
</body>
</html>
